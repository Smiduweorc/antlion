{
	"$comment": "Compiles ONLY the negative fixtures. This project is EXPECTED TO FAIL: every file under fixtures/ contains a line that must be rejected. tests/types/negative.test.ts runs tsc with this config and asserts each fixture still produces its error. The root config excludes tests, and an inherited exclude would filter the fixtures right back out, so it is overridden here.",
	"extends": "../../tsconfig.json",
	"compilerOptions": {
		"noEmit": true,
		"noUnusedLocals": false,
		"rootDir": "../../"
	},
	"include": ["./fixtures/**/*.ts", "../../index.ts", "../../legacy.ts", "../../src/**/*.ts"],
	"exclude": ["../../node_modules", "../../dist"]
}
